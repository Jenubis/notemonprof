{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ajout note pour {{ entity.cours }}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('note') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
