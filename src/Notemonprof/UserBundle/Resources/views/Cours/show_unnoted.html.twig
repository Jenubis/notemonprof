{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cours non notés</h1>

    <table class="records_list">
        <thead>
        <tr>
            <th>Id</th>
            <th>Abrv</th>
            <th>Intitule</th>
            <th>Prof</th>

        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cours_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.abrv }}</td>
                <td>{{ entity.intitule }}</td>
                <td>{{ entity.user }}</td>
                <td>

                            <a href="{{ path('note_new', { 'id_cours': entity.id }) }}">Noter</a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cours_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
